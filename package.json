{
  "name": "resolve-easing",
  "version": "0.1.0",
  "description": "Simple, beautiful easing curves for DaVinci Resolve Fusion",
  "main": "src/main/index.js",
  "scripts": {
    "dev": "electron .",
    "build": "node scripts/build.js",
    "deploy": "npm run build && node scripts/deploy.js",
    "test": "node --test src/**/*.test.js"
  },
  "author": "Massive Dynamic",
  "license": "MIT",
  "devDependencies": {
    "electron": "^28.0.0"
  }
}
